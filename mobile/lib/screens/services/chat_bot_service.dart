import 'package:flutter/material.dart';

class ChatBotService extends StatelessWidget {
  const ChatBotService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}